# Temper Assistant Notes

`AGENTS.md` is the repo source of truth. Read it first.

Temper is public and local-first.

- keep private design docs in `~/Documents/temper/docs/`
- keep repo `SESSION.md` short and public-safe
- keep `HANDOFF_*.md` local and archive long-form copies to `~/Documents/temper/docs/handoffs/`
- regenerate `derived/` when canon changes
- run `pnpm test` before claiming a repo change is done

If a change touches onboarding, continuity, or install trust, bias toward real smoke tests instead of only reasoning from code.
